# Optional overrides for OpenClaw gateway (systemd loads via EnvironmentFile=-.../gateway.env).
# Copy to: .config/openclaw/gateway.env  (same directory as vault.env)
#
# ARCHIVIST_URL=http://192.168.11.142:3100
# ARCHIVIST_SSE_READ_SECONDS=120
# MCP_AGGREGATOR=http://192.168.11.160:8080
