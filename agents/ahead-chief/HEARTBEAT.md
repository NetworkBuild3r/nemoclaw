# HEARTBEAT.md

When polled: read `HEARTBEAT.md` in workspace context. If nothing needs attention, reply `HEARTBEAT_OK`.

Optional periodic check (demo): `archivist_search` for open tasks in `tasks` and unresolved blockers in `mcp-engineering` / `firewall-ops` — only if the human enabled proactive heartbeat for this agent.
